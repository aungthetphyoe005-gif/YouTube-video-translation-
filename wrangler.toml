# Cloudflare wrangler configuration for backend worker deployment
#wrangler.toml
name = 'youtube-video'
main = "worker.ts"
compatibility_date = "2024-03-01"

# You can add global configurations here
[vars]
# NODE_ENV = "production"

# If you use custom bindings or namespaces, configure them here.
# For GEMINI_API_KEY, please add it as an encrypted secret:
# Run: npx wrangler secret put GEMINI_API_KEY
